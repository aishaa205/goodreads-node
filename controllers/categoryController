const { crudControllers } = require("../utils/crudController");
const { searchController } = require("../utils/searchController");
const Category = require("../models/category");

const categoryControllers = {
  ...crudControllers(Category),
  searchCategories: searchController(Category, "name"),
};

module.exports = categoryControllers;
